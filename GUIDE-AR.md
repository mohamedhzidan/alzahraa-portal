# الدليل الكامل — بوابة شركة الزهراء للمقاولات العامة

**مكتوب لشخص لا يعرف أي شيء عن البرمجة أو المواقع.** لا تتخطَّ أي خطوة، واقرأ بالترتيب.

---

## المحتويات

1. [ما الذي حصلت عليه بالضبط](#1)
2. [جرّب الموقع الآن على جهازك (٣٠ ثانية، بدون إنترنت)](#2)
3. [حسابات الدخول التجريبية](#3)
4. [جولة داخل النظام — ماذا يوجد في كل شاشة](#4)
5. [كيف يعمل مسار الاعتماد (أهم فكرة في النظام)](#5)
6. [نشر الموقع على الإنترنت عبر GitHub — خطوة بخطوة](#6)
7. [ربط اسم نطاق خاص بك (alzahraa.com)](#7)
8. [كيف تعدّل الموقع](#8)
9. [كيف تنشر التعديلات (التحديث)](#9)
10. [النسخ الاحتياطي — اقرأ هذا القسم مرتين](#10)
11. [الحدود الحقيقية للوضع التجريبي — مهم جداً](#11)
12. [الانتقال لقاعدة بيانات حقيقية (المرحلة الثانية)](#12)
13. [النشر على App Store و Google Play](#13)
14. [التكاليف المتوقعة](#14)
15. [الأمان والخصوصية](#15)
16. [حل المشاكل الشائعة](#16)
17. [قاموس المصطلحات](#17)
18. [خارطة الطريق المقترحة](#18)

---

<a name="1"></a>
## ١. ما الذي حصلت عليه بالضبط

حصلت على **مجلد اسمه `alzahraa-portal`** يحتوي على موقع كامل يعمل. محتوياته:

```
alzahraa-portal/
├── index.html              ← ملف البداية. هذا هو "الموقع"
├── manifest.webmanifest    ← يجعل الموقع قابلاً للتثبيت كتطبيق على الموبايل
├── .nojekyll               ← ملف فارغ ضروري لـ GitHub (لا تحذفه)
├── GUIDE-AR.md             ← هذا الدليل
├── GUIDE-EN.md             ← نفس الدليل بالإنجليزية
├── README.md               ← ملخص سريع
└── assets/
    ├── css/styles.css      ← كل الألوان والتنسيقات والمظهر
    ├── img/                ← الأيقونات
    └── js/                 ← "عقل" النظام
        ├── i18n.js         ← كل الكلمات بالعربي والإنجليزي
        ├── schema.js       ← ⭐ تعريف كل الشاشات والحقول (أهم ملف لك)
        ├── store.js        ← تخزين البيانات
        ├── auth.js         ← المستخدمون والأدوار والصلاحيات
        ├── workflow.js     ← مسار الاعتماد (مُدخل ← مراجع ← معتمد)
        ├── seed.js         ← البيانات التجريبية
        ├── ui.js           ← الأزرار والجداول والنوافذ
        ├── app.js          ← القائمة والتنقل
        └── pages/          ← لوحة التحكم، الاعتمادات، التقارير، الإعدادات
```

### ماذا يشمل النظام؟

**٣٥ شاشة عمل كاملة** موزعة على ثلاث إدارات:

| الإدارة | الشاشات |
|---|---|
| **المالية والمشتريات والمخازن** (١٦ شاشة) | دليل الحسابات، قيود اليومية، الموردون، العملاء، بنود التكلفة، اعتمادات الشراء، إذون الاستلام والفحص، فواتير الموردين، سندات الصرف، سندات القبض، الخزائن والبنوك، الأصناف، المخازن، إذون الصرف، التحويلات المخزنية، الجرد والتسويات |
| **المشروعات والمكتب الفني** (١١ شاشة) | المشروعات، موازنات المشروعات، عقود العملاء، مستخلصات العملاء، مقاولو الباطن، عقود الباطن، مستخلصات الباطن، الرسومات والمكتب الفني، التقارير اليومية للمواقع، المعدات والسيارات، حركة وصيانة المعدات |
| **الموارد البشرية والإدارة** (٨ شاشات) | الموظفون، الحضور والانصراف، طلبات الإجازات، مسير الرواتب، الشؤون القانونية، أصول تقنية المعلومات، طلبات الدعم الفني، التعميمات الإدارية |

**بالإضافة إلى:** لوحة تحكم برسوم بيانية، صندوق اعتمادات، ١٠ تقارير، إعدادات، سجل مراجعة كامل، ١٣ دوراً وظيفياً، عربي/إنجليزي بضغطة زر، وضع ليلي، تصدير Excel، طباعة، نسخ احتياطي.

**النظام مبني بالكامل على الوثيقة التي أرسلتها** — نفس مسار المستند، نفس قاعدة فصل المهام، نفس منطق العكس بدل الحذف، نفس بنية التكاليف.

---

<a name="2"></a>
## ٢. جرّب الموقع الآن على جهازك (٣٠ ثانية، بدون إنترنت)

1. افتح مجلد `alzahraa-portal`.
2. اضغط **دبل كليك** على ملف `index.html`.
3. سيفتح الموقع في متصفحك مباشرة.
4. اكتب في اسم المستخدم: `admin` وكلمة المرور: `1234` — أو اضغط أي زر من الأزرار الرمادية أسفل الشاشة لتسجيل دخول سريع.

**تم. الموقع يعمل.** لا تحتاج إنترنت ولا خادم ولا تثبيت أي برنامج.

> **ملاحظة:** الرسوم البيانية في لوحة التحكم تحتاج إنترنت (تُحمَّل من مكتبة خارجية). كل شيء آخر يعمل بدون إنترنت تماماً.

---

<a name="3"></a>
## ٣. حسابات الدخول التجريبية

**كلمة المرور لكل الحسابات: `1234`**

| اسم المستخدم | الاسم | الدور | ماذا يستطيع |
|---|---|---|---|
| `admin` | محمد زيدان | مسؤول النظام | كل شيء + إدارة المستخدمين |
| `gm` | م. أحمد الزهراء | المدير العام | اطلاع كامل + الاعتماد النهائي لكل شيء |
| `finance` | أ. سامي عبد الله | المدير المالي | اعتماد المستندات المالية + كل التقارير |
| `accountant` | أ. مروة حسن | محاسب | إدخال القيود والفواتير والسندات (لا يعتمد) |
| `purchase` | أ. كريم فؤاد | مسؤول مشتريات | إعداد اعتمادات الشراء والموردين |
| `store` | أ. رمضان السيد | أمين مخزن | استلام، صرف، تحويل، جرد |
| `pm` | م. طارق منصور | مدير مشروع | تكلفة المشروع، المستخلصات، تقارير الموقع |
| `technical` | م. نورهان صلاح | المكتب الفني | الرسومات، الموازنات، حصر الكميات |
| `hr` | أ. هالة مصطفى | موارد بشرية | الموظفون، الحضور، الإجازات، الرواتب |
| `legal` | أ. عمرو الشناوي | الشؤون القانونية | العقود، التراخيص، القضايا |
| `it` | أ. يوسف عادل | تقنية المعلومات | الأصول التقنية وطلبات الدعم |
| `auditor` | أ. إيهاب راشد | مراجع داخلي | **قراءة فقط** — يرى كل شيء ولا يعدّل أي شيء |
| `employee` | أ. سلمى إبراهيم | موظف عادي | التعميمات + تقديم إجازة أو طلب دعم فقط |

**جرّب هذا لتفهم قوة النظام:** ادخل بـ `purchase` وأنشئ اعتماد شراء وأرسله للمراجعة. ثم اخرج وادخل بـ `pm` وستجده في «صندوق الاعتمادات». راجعه. ثم اخرج وادخل بـ `finance` واعتمده. جرّب أن تراجع بنفس المستخدم الذي أنشأ المستند — النظام سيمنعك.

---

<a name="4"></a>
## ٤. جولة داخل النظام

### القائمة الجانبية
مقسّمة لمجموعات: الرئيسية، المالية، المشروعات، الموارد البشرية، النظام. **ما تراه يعتمد على دورك** — الموظف العادي يرى ٥ شاشات فقط، ومسؤول النظام يرى ٣٥.

### الشريط العلوي
- 🔍 **بحث سريع** — أو اضغط `Ctrl + K` من أي مكان، اكتب اسم أي شاشة واضغط Enter.
- 📥 **صندوق الاعتمادات** — الرقم الأحمر = عدد المستندات المنتظرة منك.
- 🌙 **الوضع الليلي**.
- **English / العربية** — تبديل اللغة الكامل بضغطة واحدة (الاتجاه والخطوط والأرقام تتغير كلها).
- **قائمة المستخدم** — الملف الشخصي، الإعدادات، نسخة احتياطية، استعادة، خروج.

### أي شاشة عمل تحتوي على:
- زر **جديد** لإضافة سجل.
- **بحث** فوري + **فلاتر حسب الحالة** (مسودة/بانتظار المراجعة/معتمد/...).
- **جدول** بأعمدة قابلة للترتيب بالضغط على العنوان + ترقيم صفحات.
- **تصدير Excel** و **طباعة**.
- على كل صف: 👁 عرض · ✏️ تعديل · 📋 نسخ كمستند جديد · 🗑 حذف.

### النماذج
مقسّمة لأقسام (البيانات الأساسية، القيم المالية، الربط والتحميل، التواريخ...). الحقول المطلوبة عليها نجمة حمراء. المستندات ذات البنود (مثل اعتماد الشراء) فيها **جدول بنود** يحسب الإجمالي والضريبة تلقائياً أثناء الكتابة.

### لوحة التحكم
٨ مؤشرات رئيسية (مشروعات نشطة، موازنات، تكلفة فعلية، التزامات، مستحقات العملاء، مستحقات الموردين، قيمة المخزون، مستندات منتظرة) + تنبيهات تلقائية (تجاوز موازنة، أصناف تحت حد الطلب، عقود قاربت الانتهاء) + رسمان بيانيان + جدول حالة المشروعات + أحدث المستندات + إجراءات سريعة.

### التقارير (١٠ تقارير)
الموازنة مقابل الفعلي · تكلفة المشروع التفصيلية · أرصدة المخزون · أعمار ديون الموردين · أعمار ديون العملاء · حركة الخزينة والبنوك · ميزان المراجعة · مستخلصات مقاولي الباطن · ملخص الرواتب · ملخص الحضور.

كلها قابلة للتصفية بالتاريخ والمشروع، وقابلة للتصدير إلى Excel والطباعة.

---

<a name="5"></a>
## ٥. كيف يعمل مسار الاعتماد (أهم فكرة في النظام)

```
مسودة ──إرسال──▶ بانتظار المراجعة ──مراجعة──▶ تمت المراجعة ──اعتماد──▶ معتمد
                        │                          │                    │
                        ├──إعادة──▶ معاد للتعديل ◀─┤                    │
                        └──رفض────▶ مرفوض ◀────────┘                    │
                                                                        └──عكس──▶ معكوس
```

### القواعد المفروضة بالقوة (لا يمكن تجاوزها إطلاقاً):

1. **من ينشئ المستند لا يمكنه مراجعته** — حتى لو كان يحمل صلاحية المراجعة.
2. **من يراجع المستند لا يمكنه اعتماده** — حتى لو كان يحمل صلاحية الاعتماد.
3. **الرفض والإعادة يتطلبان سبباً مكتوباً** إلزامياً.
4. **المستند بعد الإرسال يُقفل** ولا يُعدَّل حتى يُعاد رسمياً للمُدخل.
5. **المستند المعتمد لا يُحذف أبداً.** التصحيح الوحيد المسموح هو **«عكس المستند»**: النظام ينشئ مستنداً عكسياً بقيم سالبة مرتبطاً بالأصلي، ويبقى الأصلي محفوظاً بحالة «معكوس».
6. **كل خطوة تُسجَّل** باسم الشخص والتاريخ والوقت والسبب في «مسار الاعتماد» أسفل كل مستند، وفي «سجل المراجعة» في الإعدادات.

هذه هي بالضبط قواعد فصل المهام في الوثيقة التي أرسلتها.

---

<a name="6"></a>
## ٦. نشر الموقع على الإنترنت عبر GitHub — خطوة بخطوة

**النتيجة:** رابط مثل `https://mohamedzidan.github.io/alzahraa-portal/` يفتح من أي جهاز في العالم. **مجاني تماماً وإلى الأبد.**

### ما هو GitHub؟
موقع مجاني لتخزين ملفات المواقع والبرامج. لديه خدمة اسمها **GitHub Pages** تحوّل ملفاتك إلى موقع حقيقي على الإنترنت بضغطة زر. لا تحتاج أن تفهم أكثر من ذلك.

---

### الخطوة ١ — إنشاء حساب (٣ دقائق)

1. افتح المتصفح واذهب إلى **github.com**
2. اضغط **Sign up** (أعلى اليمين).
3. اكتب بريدك الإلكتروني ← **Continue**
4. اختر كلمة مرور قوية ← **Continue**
5. اختر اسم مستخدم (**سيظهر في رابط موقعك**، فاخترْه بعناية — مثلاً `alzahraa-contracting` أو `mohamedzidan`) ← **Continue**
6. أجب عن سؤال التحقق (puzzle) ← **Create account**
7. افتح بريدك، انسخ الكود المكوّن من ٨ أرقام، والصقه في الموقع.
8. لو سألك عن الخطة اختر **Free**.

✅ صار لديك حساب.

---

### الخطوة ٢ — إنشاء مستودع (Repository) (٢ دقيقة)

المستودع = مجلد على الإنترنت يحفظ ملفات موقعك.

1. من أعلى اليمين اضغط علامة **+** ← **New repository**
2. **Repository name:** اكتب `alzahraa-portal` (بدون مسافات، حروف إنجليزية صغيرة)
3. **Description:** (اختياري) `بوابة موظفي شركة الزهراء للمقاولات`
4. اختر:
   - **Public** = أي شخص يستطيع رؤية الكود، والموقع يعمل مجاناً ✅
   - **Private** = الكود مخفي، **لكن GitHub Pages لن يعمل مجاناً** (يحتاج اشتراك Pro بـ ~$4/شهر)

   👉 **اختر Public الآن.** الكود ليس فيه أي سر، والبيانات ليست داخل الكود أصلاً. (اقرأ قسم الأمان لاحقاً.)
5. **لا تضع علامة** على `Add a README file` ولا أي خيار آخر.
6. اضغط **Create repository**.

---

### الخطوة ٣ — رفع الملفات (٥ دقائق)

سترى صفحة فيها كود وأوامر — **تجاهلها كلها**. ابحث عن السطر:

> *"…or **uploading an existing file**"*

اضغط على **uploading an existing file** (رابط أزرق).

الآن الأهم — **انتبه لهذه النقطة جيداً:**

1. افتح مجلد `alzahraa-portal` على جهازك.
2. **حدد كل ما بداخله** (`Ctrl+A` في ويندوز أو `Cmd+A` في ماك):
   `index.html` + `manifest.webmanifest` + `.nojekyll` + `README.md` + `GUIDE-AR.md` + `GUIDE-EN.md` + **مجلد `assets`**
3. **اسحبها بالماوس** وأفلتها داخل المربع المنقّط في صفحة GitHub.

> ⚠️ **الخطأ الأشهر:** أن تسحب مجلد `alzahraa-portal` نفسه بدل محتوياته. النتيجة: يصبح رابط موقعك `.../alzahraa-portal/alzahraa-portal/` وتظهر صفحة 404.
> **الصحيح:** ادخل المجلد أولاً، ثم حدد ما بداخله واسحبه.

> ⚠️ **ملف `.nojekyll`** يبدأ بنقطة فقد يكون مخفياً:
> - **ويندوز:** في نافذة المجلد ← تبويب `View` ← علّم على `Hidden items`
> - **ماك:** اضغط `Cmd + Shift + .` (نقطة)
>
> لو لم تجده، لا تقلق — سأشرح كيف تنشئه على GitHub مباشرة في نهاية هذه الخطوة.

4. انتظر حتى يكتمل رفع كل الملفات (سترى قائمة بها).
5. في الأسفل، في خانة **Commit changes**، اكتب: `النسخة الأولى`
6. اضغط الزر الأخضر **Commit changes**.

**لإنشاء `.nojekyll` مباشرة على GitHub لو لم يُرفع:**
اضغط **Add file** ← **Create new file** ← اكتب في خانة الاسم `.nojekyll` ← اترك المحتوى فارغاً ← **Commit changes**.

---

### الخطوة ٤ — تشغيل GitHub Pages (٢ دقيقة)

1. من أعلى صفحة المستودع اضغط تبويب **Settings** (⚙️).
2. من القائمة اليسرى اضغط **Pages**.
3. تحت **Source** اختر **Deploy from a branch**.
4. تحت **Branch**: اختر `main` من القائمة الأولى، و `/ (root)` من الثانية.
5. اضغط **Save**.

---

### الخطوة ٥ — انتظر ثم افتح موقعك (١–٥ دقائق)

1. ابقَ في نفس صفحة **Pages** وحدّثها (F5) كل دقيقة.
2. سيظهر مربع أخضر:
   > ✅ **Your site is live at** `https://اسم-المستخدم.github.io/alzahraa-portal/`
3. اضغط الرابط. **موقعك الآن على الإنترنت.**

> ⏳ أول مرة قد تأخذ حتى ١٠ دقائق. لو ظهرت صفحة 404، انتظر وحدّث. لو استمرت، راجع [حل المشاكل](#16).

### الخطوة ٦ — شارك الرابط
أرسل الرابط للموظفين. يفتح على أي جهاز: كمبيوتر، موبايل، تابلت.

**على الموبايل يمكنهم تثبيته كتطبيق:**
- **أندرويد (Chrome):** القائمة (⋮) ← *Add to Home screen*
- **آيفون (Safari):** زر المشاركة ⬆️ ← *Add to Home Screen*

سيظهر بأيقونة على الشاشة الرئيسية ويفتح بملء الشاشة كأنه تطبيق حقيقي.

---

<a name="7"></a>
## ٧. ربط اسم نطاق خاص بك

بدل `mohamedzidan.github.io/alzahraa-portal` تريد `portal.alzahraa-contracting.com`.

1. اشترِ نطاقاً من **Namecheap** أو **GoDaddy** أو **Cloudflare** (حوالي **$10–15 سنوياً**).
2. في لوحة تحكم مزوّد النطاق، افتح **DNS Settings** وأضف:
   - سجل من نوع **CNAME**
   - **Name/Host:** `portal`
   - **Value/Target:** `اسم-المستخدم.github.io`
3. ارجع لـ GitHub ← **Settings** ← **Pages** ← خانة **Custom domain** ← اكتب `portal.alzahraa-contracting.com` ← **Save**.
4. انتظر من ١٥ دقيقة إلى ٢٤ ساعة.
5. عندما يسمح لك، علّم على **Enforce HTTPS** (يجعل الرابط آمناً 🔒).

---

<a name="8"></a>
## ٨. كيف تعدّل الموقع

### طريقتان للتعديل

**الطريقة أ — مباشرة على GitHub (الأسهل، بدون أي برامج):**
1. افتح مستودعك على github.com
2. ادخل للمجلد واضغط على الملف المطلوب
3. اضغط أيقونة القلم ✏️ أعلى اليمين
4. عدّل النص
5. انزل للأسفل ← **Commit changes**
6. انتظر دقيقة، حدّث موقعك — التعديل ظهر ✅

**الطريقة ب — على جهازك (أفضل للتعديلات الكبيرة):**
1. عدّل الملفات على جهازك بمحرر نصوص. حمّل **VS Code** مجاناً من `code.visualstudio.com` (أفضل بكثير من Notepad).
2. جرّب التعديل محلياً بفتح `index.html`.
3. ارفع النسخة الجديدة كما في [الخطوة ٩](#9).

---

### تعديل ١: اسم الشركة وبياناتها
**لا تحتاج تعديل كود أصلاً.** ادخل بـ `admin` ← **الإعدادات** ← **بيانات الشركة** ← عدّل ← **حفظ**.

لتغيير الاسم الظاهر في شاشة الدخول والقائمة، افتح `assets/js/i18n.js` وعدّل:
```javascript
'app.name': 'شركة الزهراء للمقاولات العامة',   // غيّر هنا
'app.short': 'الزهراء للمقاولات',              // وهنا
```
ثم نفس المفاتيح في قسم `en:` بالإنجليزية.

---

### تعديل ٢: الألوان
افتح `assets/css/styles.css`. أول ٢٠ سطر:
```css
--green-800:#0b3d2e;   /* اللون الأساسي الداكن */
--green-700:#12543f;   /* لون الأزرار */
--gold-500:#c9a227;    /* اللون الذهبي المميز */
```
غيّر الأكواد (استخدم موقع `htmlcolorcodes.com` لاختيار لون ونسخ كوده). التغيير يسري على الموقع كله فوراً.

---

### تعديل ٣: الشعار
1. جهّز شعارك بصيغة **PNG** أو **SVG** بخلفية شفافة.
2. ارفعه في `assets/img/` باسم `logo.png`.
3. افتح `index.html`، ابحث عن `<svg viewBox="0 0 64 64" width="52"` (يوجد مرتان: شاشة الدخول والقائمة الجانبية).
4. استبدل كتلة `<svg>...</svg>` كلها بـ:
   ```html
   <img src="assets/img/logo.png" alt="شعار الشركة" width="52">
   ```

---

### تعديل ٤: إضافة حقل جديد لشاشة موجودة ⭐

هذا أكثر تعديل ستحتاجه. مثال: إضافة حقل «رقم أمر التوريد» لشاشة اعتمادات الشراء.

افتح `assets/js/schema.js`، ابحث عن `id: 'purchaseApprovals'`، وداخل مصفوفة `fields:` أضف سطراً:

```javascript
F('poNumber', 'رقم أمر التوريد', 'PO number', 'text', { section: SEC.main }),
```

**تفسير كل جزء:**
| الجزء | المعنى |
|---|---|
| `'poNumber'` | الاسم الداخلي — إنجليزي، بدون مسافات، فريد داخل الشاشة |
| `'رقم أمر التوريد'` | ما يراه المستخدم بالعربي |
| `'PO number'` | ما يراه المستخدم بالإنجليزي |
| `'text'` | نوع الحقل |
| `{ section: SEC.main }` | في أي قسم من النموذج يظهر |

**أنواع الحقول المتاحة:**
`text` نص · `textarea` نص طويل · `number` رقم · `money` مبلغ · `percent` نسبة · `date` تاريخ · `select` قائمة اختيار · `ref` ربط بشاشة أخرى · `checkbox` نعم/لا · `email` · `phone` · `calc` حقل محسوب بمعادلة

**خيارات إضافية:**
```javascript
{ required: true }              // حقل إلزامي
{ full: true }                  // يأخذ عرض السطر كاملاً
{ default: 'today' }            // قيمة افتراضية (أو رقم أو نص)
{ readonly: true }              // للعرض فقط
{ help: {ar:'تلميح', en:'hint'} } // نص إرشادي أسفل الحقل
```

**لقائمة اختيار:**
```javascript
F('urgency', 'درجة الاستعجال', 'Urgency', 'select', {
  section: SEC.main,
  options: [
    { value: 'low',  label: { ar: 'منخفضة', en: 'Low' } },
    { value: 'high', label: { ar: 'عالية',  en: 'High' } }
  ]
}),
```

**لربط بشاشة أخرى:**
```javascript
F('engineer', 'المهندس المسؤول', 'Responsible engineer', 'ref',
  { ref: 'employees', refLabel: 'name', section: SEC.main }),
```

**لإظهار الحقل في الجدول** أضف اسمه لمصفوفة `columns` في نفس الشاشة:
```javascript
columns: ['docNo', 'date', 'poNumber', 'project', 'grandTotal', 'status'],
```

**هذا كل شيء.** النموذج والجدول والبحث والتصدير والصلاحيات تتحدث كلها تلقائياً.

---

### تعديل ٥: إضافة شاشة جديدة بالكامل

مثال: شاشة «السلامة والصحة المهنية». افتح `schema.js`، وقبل السطر `];` الذي ينهي `var MODULES`، أضف:

```javascript
,{
  id: 'hse', table: 'hse', group: 'projects', icon: 'alert',
  label: { ar: 'السلامة والصحة المهنية', en: 'Health & Safety' },
  desc: { ar: 'بلاغات وحوادث وتفتيش السلامة', en: 'Incidents and safety inspections' },
  docPrefix: 'HSE',
  search: ['docNo', 'title'],
  columns: ['docNo', 'date', 'project', 'severity', 'title'],
  fields: [
    F('date', 'التاريخ', 'Date', 'date', { required: true, default: 'today', section: SEC.main }),
    F('title', 'الموضوع', 'Subject', 'text', { required: true, section: SEC.main, full: true }),
    F('project', 'المشروع', 'Project', 'ref', { ref: 'projects', refLabel: 'name', required: true, section: SEC.main }),
    F('severity', 'درجة الخطورة', 'Severity', 'select', {
      required: true, section: SEC.main, options: [
        { value: 'near',   label: { ar: 'حادث وشيك', en: 'Near miss' } },
        { value: 'minor',  label: { ar: 'إصابة بسيطة', en: 'Minor injury' } },
        { value: 'major',  label: { ar: 'إصابة بليغة', en: 'Major injury' } }
      ]
    }),
    F('description', 'الوصف', 'Description', 'textarea', { required: true, section: SEC.main, full: true }),
    F('action', 'الإجراء المتخذ', 'Corrective action', 'textarea', { section: SEC.extra, full: true })
  ]
}
```

ثم افتح `assets/js/auth.js` وأعطِ الأدوار صلاحية عليها، مثلاً داخل `project_manager`:
```javascript
hse: ['view', 'create', 'edit', 'delete'],
```
و داخل `admin` لا تحتاج شيئاً (لديه `'*'`).

**لجعل الشاشة تتبع مسار الاعتماد** أضف `workflow: true` بجانب `docPrefix`.

**المجموعات المتاحة لـ `group`:** `finance` أو `projects` أو `people`.

---

### تعديل ٦: إضافة أو تعديل مستخدم
ادخل بـ `admin` ← **الإعدادات** ← **المستخدمون والصلاحيات** ← **جديد**. اختر الدور، وإن أردت حصر المستخدم على مشروعات محددة علّم عليها.

**لتعديل صلاحيات دور بالكامل** افتح `assets/js/auth.js` وعدّل مصفوفة `perms` للدور. الإجراءات المتاحة: `view`, `create`, `edit`, `delete`, `review`, `approve`.

---

### تعديل ٧: تغيير أي كلمة في الواجهة
كل الكلمات في `assets/js/i18n.js`. ابحث عن الكلمة العربية وغيّرها، ثم غيّر مقابلها في قسم `en:`.

---

<a name="9"></a>
## ٩. كيف تنشر التعديلات (التحديث)

### إذا عدّلت مباشرة على GitHub
لا شيء إضافي — بمجرد الضغط على **Commit changes** ينشر التعديل خلال دقيقة تقريباً.

### إذا عدّلت على جهازك
1. افتح مستودعك على github.com
2. ادخل للمجلد الذي فيه الملف المعدّل (مثلاً `assets` ← `js`)
3. اضغط **Add file** ← **Upload files**
4. اسحب الملف الجديد وأفلته — **سيستبدل القديم تلقائياً بنفس الاسم**
5. **Commit changes**
6. انتظر دقيقة وحدّث موقعك

> 💡 **لو لم تر التعديل:** اضغط `Ctrl + Shift + R` (أو `Cmd + Shift + R` على ماك) لتحديث قوي يتجاوز ذاكرة المتصفح المؤقتة.

### التراجع عن تعديل خاطئ
GitHub يحفظ **كل نسخة سابقة إلى الأبد**. اضغط تبويب **Commits** (أو أيقونة الساعة 🕐)، اختر النسخة التي كانت تعمل، اضغط **Browse files**، ثم انسخ محتوى الملف القديم والصقه مكان الجديد. لن تفقد شيئاً أبداً.

---

<a name="10"></a>
## ١٠. النسخ الاحتياطي — اقرأ هذا القسم مرتين

### 🔴 الحقيقة الأهم في هذا الدليل كله

في الوضع الحالي (التجريبي)، **البيانات محفوظة داخل متصفح كل مستخدم على جهازه فقط.** ليست على الإنترنت وليست مشتركة.

**هذا يعني:**
- ما يدخله المحاسب على جهازه **لا يراه** مدير المشروع على جهازه.
- مسح ذاكرة المتصفح (Clear browsing data) = **ضياع كل البيانات**.
- تغيير الجهاز أو المتصفح = تبدأ من الصفر.
- استخدام وضع التصفح المتخفي (Incognito) = تضيع البيانات بمجرد إغلاق النافذة.
- المساحة محدودة بحوالي **٥ ميجابايت** (تكفي آلاف السجلات النصية، لكن ليست للملفات والصور).

### كيف تأخذ نسخة احتياطية
قائمة المستخدم (أعلى) ← **نسخة احتياطية (تنزيل)** ← سيُحفظ ملف باسم `alzahraa-backup-2026-08-10.json`.

### كيف تستعيدها
قائمة المستخدم ← **استعادة نسخة** ← اختر الملف. سيُعاد تحميل النظام بالبيانات المستعادة.

### 📌 قاعدة ذهبية
> **خذ نسخة احتياطية كل يوم عمل، واحفظها على Google Drive أو OneDrive أو بريدك.**

### استخدام النسخة الاحتياطية للمزامنة اليدوية
حتى تنتقل لقاعدة بيانات حقيقية، يمكنك عملياً: شخص واحد فقط يُدخل البيانات، يأخذ نسخة احتياطية في نهاية اليوم، ويرسلها للآخرين ليستعيدوها. **هذا حل مؤقت ومزعج** — وهو بالضبط السبب الذي يجعل المرحلة الثانية ضرورية.

---

<a name="11"></a>
## ١١. الحدود الحقيقية للوضع التجريبي — مهم جداً

كن صريحاً مع نفسك ومع الإدارة بشأن ما يصلح له هذا النظام الآن:

### ✅ ممتاز لـ:
- **عرض النظام على الإدارة** والحصول على موافقة واعتماد التصميم.
- **تدريب الموظفين** على الشاشات ومسار الاعتماد قبل التشغيل الحقيقي.
- **اختبار المنطق**: هل الحقول كافية؟ هل التقارير هي المطلوبة؟ هل مسار الاعتماد منطقي؟
- **وثيقة حية** تعطيها لأي مبرمج بدل الشرح الكلامي — أفضل بمراحل من مواصفات على ورق.

### ❌ لا يصلح لـ:
- **بيانات مالية حقيقية** يعتمد عليها في اتخاذ قرار أو مراجعة.
- **عمل عدة أشخاص معاً** — لا توجد مزامنة إطلاقاً.
- **الأمان** — كلمات المرور مكتوبة نصاً عادياً داخل الكود، وأي شخص لديه معرفة بسيطة يمكنه رؤيتها وتعديل البيانات من متصفحه.
- **الملفات والمرفقات** — يمكنك حفظ رابط لملف على Drive، لا الملف نفسه.
- **الطباعة الرسمية للفواتير الضريبية** أو الربط بمنظومة الفاتورة الإلكترونية.

### 💬 كيف تشرح هذا للإدارة
> «هذا نموذج تشغيلي كامل يوضح النظام النهائي بالضبط: كل الشاشات، كل الحقول، مسار الاعتماد، والتقارير. نستخدمه لاعتماد التصميم وتدريب الفريق. المرحلة التالية هي ربطه بقاعدة بيانات حقيقية ليعمل عليه الجميع في نفس الوقت بأمان.»

---

<a name="12"></a>
## ١٢. الانتقال لقاعدة بيانات حقيقية (المرحلة الثانية)

### لماذا؟
لكي: يعمل كل الموظفين على **نفس البيانات في نفس اللحظة**، وتكون كلمات المرور **مشفّرة**، وتُحفظ البيانات على خادم مع **نسخ احتياطي تلقائي**، وتُرفع **مرفقات حقيقية**.

### الحل الأنسب: **Supabase**
قاعدة بيانات جاهزة على الإنترنت، **مجانية** حتى ٥٠٠ ميجابايت — أكثر من كافٍ لسنوات لشركة بحجمكم.

### 🎁 الخبر الجيد
**كتبت لك النظام مُهيَّأً لهذا من البداية.** كل التعامل مع البيانات يمر عبر ملف واحد فقط هو `assets/js/store.js`، وفيه هيكل `SupabaseAdapter` جاهز (مكتوب كتعليق). المطلوب استبدال محرك التخزين فقط — **لن تتغير أي شاشة ولا أي حقل ولا أي تقرير.**

### الخطوات المختصرة (لك أو للمبرمج)
1. أنشئ حساباً مجانياً على **supabase.com** ← **New project**.
2. من قائمة **Table Editor** أنشئ جدولاً لكل `table` مذكور في `schema.js` (٣٧ جدولاً)، والأعمدة هي `name` الخاصة بكل حقل.
3. من **Authentication** فعّل الدخول بالبريد الإلكتروني وأنشئ حسابات الموظفين.
4. من **Settings ← API** انسخ `Project URL` و `anon public key`.
5. في `index.html` أضف قبل باقي السكربتات:
   ```html
   <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
   ```
6. في `store.js` فعّل كتلة `SupabaseAdapter` (احذف علامات التعليق `/*` و `*/`)، وضع الـ URL والمفتاح، ثم استبدل السطر:
   ```javascript
   var adapter = LocalAdapter;
   ```
   بـ:
   ```javascript
   var adapter = SupabaseAdapter;
   SupabaseAdapter.connect('ضع-الرابط-هنا', 'ضع-المفتاح-هنا');
   ```
7. فعّل **Row Level Security** في Supabase لتطبيق الصلاحيات على مستوى قاعدة البيانات نفسها (مهم جداً للأمان الحقيقي).

### ⏱ التقدير الواقعي
مبرمج متمكن ينهي هذا في **٣ إلى ٧ أيام عمل**. التكلفة في السوق المصري تقريباً **١٥٬٠٠٠ – ٤٠٬٠٠٠ جنيه**. وهذا أرخص بكثير من بناء النظام من الصفر، لأن **٩٠٪ من العمل جاهز أمامه** — الشاشات والمنطق والتقارير والصلاحيات كلها مكتوبة.

---

<a name="13"></a>
## ١٣. النشر على App Store و Google Play

### الطريق الأول: PWA (تطبيق ويب) — **مجاني وفوري وأنصح به الآن** ⭐

الموقع **جاهز بالفعل** لهذا (بفضل ملف `manifest.webmanifest`). الموظف يفتح الرابط ويضيفه للشاشة الرئيسية فيحصل على أيقونة وتجربة تطبيق كاملة.

| | |
|---|---|
| **التكلفة** | صفر |
| **الوقت** | ٣٠ ثانية للموظف |
| **الميزة** | التحديث فوري للجميع بمجرد رفعك للتعديل — لا مراجعة ولا انتظار |
| **العيب** | ليس داخل المتاجر، ولا يصل لإشعارات iOS بشكل كامل |

**للاستخدام الداخلي في شركة، هذا هو الخيار الصحيح تماماً.**

---

### الطريق الثاني: تطبيق حقيقي في المتاجر

يتم بتغليف الموقع داخل تطبيق باستخدام **Capacitor** أو **Median.co**.

#### Google Play (أندرويد)
1. رسوم تسجيل مطوّر: **$25 مرة واحدة مدى الحياة**.
2. تغليف الموقع (يوم عمل لمبرمج، أو خدمة جاهزة).
3. تجهيز: أيقونة، لقطات شاشة، وصف، **سياسة خصوصية على رابط عام** (إلزامية).
4. الرفع والمراجعة: **من يوم إلى ٧ أيام**.
5. ⚠️ Google تشدّد على التطبيقات التي هي «مجرد موقع مغلّف» وقد ترفضها. الحل: أن يستخدم التطبيق ميزة أصلية حقيقية (إشعارات، كاميرا لتصوير المستندات، عمل بدون إنترنت).

#### Apple App Store (آيفون)
1. رسوم: **$99 سنوياً متجددة**.
2. **تحتاج جهاز Mac** لبناء التطبيق ورفعه (أو خدمة سحابية بديلة).
3. المراجعة **أكثر صرامة**، ورفض التطبيقات المغلّفة شائع جداً (البند 4.2 من إرشادات آبل).
4. المراجعة: **من يومين إلى أسبوعين**، وقد ترفض عدة مرات.

#### 🔑 البديل الذكي للاستخدام الداخلي
لو الهدف هو موظفو الشركة فقط، فالمتاجر **إهدار للوقت والمال**:
- **أندرويد:** ولّد ملف `.apk` ووزّعه على الموظفين مباشرة (مجاني تماماً).
- **آيفون:** استخدم PWA — نفس النتيجة عملياً.
- **للشركات الكبيرة:** برنامج Apple Business Manager للتوزيع الداخلي (يحتاج حساب مؤسسي).

### 💡 نصيحتي الصريحة
**لا تفكر في المتاجر الآن إطلاقاً.** انشر على GitHub Pages، دع الموظفين يثبتونه كـ PWA، ركّز على المرحلة الثانية (قاعدة البيانات). فكّر في المتاجر بعد سنة من التشغيل الفعلي إن ظهرت حاجة حقيقية.

---

<a name="14"></a>
## ١٤. التكاليف المتوقعة

| البند | التكلفة | إلزامي؟ |
|---|---|---|
| حساب GitHub | **مجاني** | ✅ نعم |
| استضافة GitHub Pages | **مجاني للأبد** | ✅ نعم |
| شهادة HTTPS 🔒 | **مجاني** (تلقائي) | ✅ نعم |
| اسم نطاق خاص | $10–15 / سنة | ❌ اختياري |
| مستودع Private + Pages | ~$4 / شهر | ❌ اختياري |
| قاعدة بيانات Supabase | **مجاني** حتى ٥٠٠ MB، ثم $25/شهر | ⚠️ للمرحلة الثانية |
| برمجة المرحلة الثانية | ١٥٬٠٠٠–٤٠٬٠٠٠ جنيه (مرة واحدة) | ⚠️ للتشغيل الحقيقي |
| Google Play | $25 مرة واحدة | ❌ اختياري |
| Apple App Store | $99 / سنة | ❌ اختياري |

**للبدء الآن: صفر جنيه.**

---

<a name="15"></a>
## ١٥. الأمان والخصوصية

### 🔴 ما يجب أن تعرفه فوراً

1. **كلمات المرور مكشوفة.** مكتوبة نصاً عادياً في `seed.js`. أي شخص يفتح رابط موقعك يستطيع قراءتها. **غيّرها لا يفيد** — ستكون مكشوفة أيضاً.
2. **لا يوجد أمان حقيقي.** الصلاحيات في هذا الوضع تنظّم العمل وتمنع الأخطاء، لكنها **لا تحمي** من شخص يعرف ما يفعل.
3. **الكود عام (Public).** أي شخص يرى ملفاتك — وهذا عادي وآمن **طالما لا توجد بيانات حقيقية بداخلها**.

### ✅ القواعد الملزمة الآن

- **لا تدخل بيانات مالية حقيقية** قبل المرحلة الثانية.
- **لا تكتب أرقاماً قومية أو رواتب حقيقية أو أرقام حسابات بنكية** في البيانات التجريبية.
- **لا تضع أي كلمة مرور حقيقية** تستخدمها في مكان آخر.
- إذا أردت عرضاً بأسماء حقيقية للإدارة، استخدمها **محلياً على جهازك فقط** ولا ترفعها.

### 🛡 بعد المرحلة الثانية ستحصل على
تشفير كلمات المرور، صلاحيات على مستوى قاعدة البيانات (RLS)، تسجيل دخول آمن، جلسات تنتهي تلقائياً، نسخ احتياطي تلقائي، وسجل مراجعة لا يمكن العبث به.

---

<a name="16"></a>
## ١٦. حل المشاكل الشائعة

| المشكلة | السبب والحل |
|---|---|
| **صفحة 404 بعد النشر** | (١) لم تمر ٥ دقائق بعد. (٢) رفعت المجلد بدل محتوياته — تأكد أن `index.html` يظهر في **الصفحة الأولى** للمستودع وليس داخل مجلد. (٣) لم تختر `main` و `/(root)` في Settings ← Pages. |
| **الصفحة بيضاء تماماً** | ملف من مجلد `assets` لم يُرفع. تأكد أن `assets/js/` فيه ١٣ ملفاً و `assets/css/` فيه `styles.css`. |
| **التعديل لا يظهر** | ذاكرة المتصفح المؤقتة. اضغط `Ctrl+Shift+R`. أو انتظر دقيقتين. |
| **الرسوم البيانية لا تظهر** | لا يوجد إنترنت، أو لا توجد بيانات معتمدة. باقي الموقع يعمل طبيعياً. |
| **«مساحة التخزين امتلأت»** | خذ نسخة احتياطية ثم احذف سجلات قديمة، أو انتقل للمرحلة الثانية. |
| **البيانات اختفت** | مُسحت ذاكرة المتصفح أو تستخدم متصفحاً/جهازاً آخر. استعد نسختك الاحتياطية. |
| **النص يظهر مقلوباً أو مبعثراً** | حدّث المتصفح لآخر إصدار. النظام يحتاج متصفحاً حديثاً (Chrome/Edge/Safari/Firefox). |
| **لا أستطيع اعتماد مستند** | هذا مقصود. إما أنك أنشأته أو راجعته بنفسك (فصل المهام)، أو دورك لا يملك صلاحية الاعتماد. |
| **لا أرى شاشة معينة في القائمة** | دورك لا يملك صلاحية عليها. ادخل بـ `admin` وعدّل الصلاحيات. |
| **Excel يعرض العربي كرموز** | الملف يحوي BOM ويُفتح صحيحاً. لو حدث: افتح Excel ← Data ← From Text ← اختر UTF-8. |

---

<a name="17"></a>
## ١٧. قاموس المصطلحات

| المصطلح | المعنى ببساطة |
|---|---|
| **HTML** | لغة بناء صفحة الويب (الهيكل) |
| **CSS** | لغة التنسيق (الألوان والخطوط والأشكال) |
| **JavaScript / JS** | لغة الحركة والمنطق (الحسابات والأزرار) |
| **Repository / مستودع** | مجلد مشروعك على GitHub |
| **Commit** | حفظ نسخة من تعديلاتك (كأنه Ctrl+S بتاريخ ووصف) |
| **Deploy / نشر** | جعل الموقع متاحاً على الإنترنت |
| **GitHub Pages** | خدمة GitHub المجانية لاستضافة المواقع |
| **Domain / نطاق** | عنوان الموقع، مثل `alzahraa.com` |
| **DNS** | دليل يربط اسم النطاق بمكان الملفات |
| **HTTPS 🔒** | اتصال مشفّر وآمن |
| **localStorage** | مخزن صغير داخل المتصفح — مكان بياناتك الآن |
| **Backend / خلفية** | الخادم وقاعدة البيانات (غير موجود الآن) |
| **Supabase** | خدمة قاعدة بيانات جاهزة ومجانية |
| **PWA** | موقع يمكن تثبيته كتطبيق على الموبايل |
| **API** | طريقة تخاطب برنامجين مع بعضهما |
| **Cache / ذاكرة مؤقتة** | نسخة محفوظة في المتصفح لتسريع الفتح |
| **RLS** | صلاحيات مفروضة داخل قاعدة البيانات نفسها |

---

<a name="18"></a>
## ١٨. خارطة الطريق المقترحة

**الأسبوع ١ — النشر والاعتماد**
انشر على GitHub Pages · جرّب كل شاشة بكل دور · اعرضه على الإدارة · اجمع الملاحظات مكتوبة.

**الأسبوع ٢–٣ — التخصيص**
عدّل الشاشات والحقول حسب الملاحظات · أدخل بياناتك الأساسية الحقيقية (مشروعات، موردون، أصناف، دليل حسابات) · احذف البيانات التجريبية من كل شاشة · **خذ نسخة احتياطية**.

**الأسبوع ٤ — التدريب**
درّب كل موظف على شاشاته ودوره · طبّق دورة كاملة تجريبية: اعتماد شراء ← استلام ← فاتورة ← سداد · اعتمد التصميم النهائي كتابةً.

**الشهر ٢–٣ — المرحلة الثانية**
تعاقد مع مبرمج لربط Supabase (سلّمه هذا الدليل والكود) · اختبر بمشروع واحد أولاً · ثم عمّم على كل المشروعات.

**بعد ٦ أشهر — التوسع**
مرفقات حقيقية · الفاتورة الإلكترونية (ETA) · تنبيهات بالبريد · لوحات تحكم متقدمة · تطبيق موبايل إن ظهرت حاجة فعلية.

---

## ✅ قائمة تحقق سريعة

- [ ] فتحت `index.html` وجرّبت الموقع على جهازي
- [ ] دخلت بـ ٣ حسابات مختلفة على الأقل لأفهم الأدوار
- [ ] جرّبت دورة اعتماد كاملة (إنشاء ← مراجعة ← اعتماد)
- [ ] أنشأت حساب GitHub
- [ ] أنشأت مستودع `alzahraa-portal`
- [ ] رفعت **محتويات** المجلد (وليس المجلد نفسه)
- [ ] شغّلت GitHub Pages من Settings ← Pages
- [ ] فتحت الرابط ونجح ✅
- [ ] ثبّته على موبايلي كتطبيق
- [ ] أخذت أول نسخة احتياطية
- [ ] قرأت قسم [الحدود](#11) وقسم [الأمان](#15) وفهمت أنه لا يُستخدم لبيانات حقيقية بعد

---

**نظام كامل، ملكك بالكامل، بدون أي اشتراكات أو قيود. بالتوفيق. 🏗️**
